[project]
name = "ica"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michel Ferreira", email = "michelmf@poli.ufrj.br" }
]
requires-python = ">=3.14"
dependencies = [
    "ruff>=0.16.5",
    "torch>=2.13.0",
]

[project.scripts]
ica = "ica:main"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"
